name: Auto-update game/app index

on:
  push:
    branches: [ main ]
    paths:
      - 'Micro_Games/**'
      - 'Micro_Apps/**'
      - 'update_microverse.py'

permissions:
  contents: write

jobs:
  update-index:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run index generator
        run: python update_microverse.py

      - name: Commit updated index files
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add Micro_Games/index.html Micro_Apps/index.html
          git diff --staged --quiet || git commit -m "Auto-update index.html [skip ci]"
          git push

